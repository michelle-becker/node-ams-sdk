{
  "name": "node-ams-sdk",
  "version": "0.2.0",
  "description": "Client SDK for Azure Media Service",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:michelle-becker/node-ams-sdk.git"
  },
  "dependencies": {
    "moment": "^2.6.0",
    "request": "^2.34.0",
    "through": "^2.3.4"
  },
  "config": {
    "precommit": {
      "lint": true,
      "validate": true,
      "test": false
    }
  },
  "scripts": {
    "test": "mocha --recursive -R spec --ui bdd ",
    "lint": "jshint ."
  },
  "devDependencies": {
    "chai": "~1.9.0",
    "jshint": "^2.5.0",
    "precommit-hook": "^0.4.1"
  }
}
